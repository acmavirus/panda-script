# Changelog v3.0.0 (Coming Soon)

## 🎉 Web-Based Control Panel — Major Architecture Upgrade

Phiên bản 3.0 đánh dấu bước chuyển đổi mang tính cách mạng từ CLI (Command Line Interface) sang **Web Dashboard** tại địa chỉ `domain_or_ip/panda`, biến Panda Script thành một Control Panel chuyên nghiệp hoàn chỉnh.

---

### 🏗️ Kiến trúc Hệ thống (Backend & Security)

- **Panda API Service**: Backend API chạy dưới dạng service ngầm (Golang + Gin/Echo framework).
  - File binary duy nhất (~15-30MB), không cần cài đặt runtime phụ.
  - Tiêu thụ RAM cực thấp (~10-20MB).
  - Thực thi lệnh hệ thống an toàn dưới quyền root.
  - **Self-Update & Hot-reload**: Cập nhật phiên bản mới chỉ với 1 click ngay trên Dashboard, tự động tải binary từ GitHub và restart service không gián đoạn.
- **Authentication System**: Đăng nhập bảo mật với JWT/Session.
  - Hỗ trợ xác thực 2 lớp (2FA - Google Authenticator).
  - **One-Time Login Link**: Tạo link đăng nhập nhanh qua CLI (`panda panel --get-login`) hiệu lực 5 phút, hữu ích khi quên mật khẩu.
  - IP Whitelist cho trang quản trị.
- **Panel SSL**: Tự động cấp SSL cho chính trang quản trị, đảm bảo dữ liệu được mã hóa.
- **Security Path**: Đường dẫn tùy chỉnh (vd: `/panda-access-xxx`), không dùng port mặc định.
- **Command Queue & Task Runner**: Cơ chế hàng đợi lệnh thông minh xử lý các tác vụ tốn thời gian (như cài đặt website/app).
  - Sử dụng **WebSockets/SSE** để đẩy tiến độ (progress bar) về giao diện theo thời gian thực.
  - Xử lý bất đồng bộ (Non-blocking): Người dùng có thể tiếp tục thao tác khác trong khi lệnh đang chạy ngầm.

---

### 📊 Dashboard Tổng quan (Visual Analytics)

- **Real-time Charts**: Biểu đồ trực quan theo thời gian thực.
  - Mức sử dụng CPU, RAM, Disk, và Băng thông (Network).
- **Service Status Panel**: Hiển thị trạng thái các dịch vụ (Nginx, MySQL, PHP).
  - Nút Start/Stop/Restart trực quan.
- **Top Processes View**: Danh sách các tiến trình chiếm tài nguyên nhiều nhất (giao diện giống `htop`).

---

### 📂 File Manager (Quản lý tệp tin trực tuyến)

- **Web-based File Explorer**: Quản lý file hoàn chỉnh trên trình duyệt.
  - Copy, Move, Rename, Delete.
  - Chmod, Chown trực tiếp.
  - **Drag & Drop Upload**: Kéo thả file/thư mục trực tiếp từ máy tính vào trình duyệt.
- **Code Editor**: Tích hợp Monaco Editor/Ace với Syntax Highlighting.
  - Chỉnh sửa `.php`, `.env`, `.conf`, `.nginx` trực tiếp.
- **Archive Manager**: Nén/giải nén file nhanh chóng.
  - Hỗ trợ `.zip`, `.tar.gz`, `.tar.bz2`.
- **Remote Download**: Tính năng "Wget UI" - dán URL để server tự động tải file về thư mục hiện hành.

### 🐋 Visual Docker & App Store

- **Container UI**: Quản lý Docker qua giao diện đồ họa.
  - Xem logs, Restart, chỉnh sửa Environment Variables.
  - Theo dõi resource usage của từng container.
- **Panda App Store (Plugin-based)**: Kho ứng dụng không còn bị "hard-code" trong binary.
  - Mỗi ứng dụng là một file cấu hình `.json` hoặc `.yaml`.
  - Dễ dàng cập nhật danh sách ứng dụng, image version và cấu hình mà không cần build lại core binary.
  - Hỗ trợ cài đặt nhanh: Nextcloud, Ghost, WordPress, n8n, ... với Reverse Proxy tự động.

---

### 👨‍💻 Web Terminal (Console Emulator)

- **Integrated xterm.js**: Cửa sổ Terminal ngay trên trình duyệt.
  - Gõ lệnh trực tiếp mà không cần SSH client (PuTTY, Termius).
  - Hỗ trợ đa tab terminal.
- **Terminal Strict Mode**: Bảo mật tối đa cho cửa sổ dòng lệnh.
  - Xác thực phiên làm việc riêng biệt (Session-based authentication).
  - **Auto-timeout**: Tự động ngắt kết nối nếu không hoạt động sau 5-10 phút để tránh rủi ro bảo mật.

---

### 🗄️ Visual Database Management

- **Built-in DB Manager**: Không cần cài thêm phpMyAdmin.
  - Xem danh sách Tables, browse dữ liệu.
  - Chỉnh sửa records nhanh.
- **SQL Playground**: Trình soạn thảo SQL hỗ trợ **Autocomplete**.
  - Viết và chạy query trực tiếp trên giao diện để debug nhanh mà không cần tool ngoài.
- **Import/Export SQL**: Xử lý file dung lượng lớn bằng stream.
  - Tránh timeout trình duyệt.
- **Database Backup/Restore**: Một-click backup và restore.

---

### 👥 User Roles & Resource Management

- **Multi-User Support**: Phân quyền Admin và User.
  - Admin: Toàn quyền hệ thống.
  - User: Chỉ quản lý website được chỉ định.
- **Resource Limits**: Giới hạn tài nguyên cho từng Website/User.
  - Dung lượng ổ đĩa (Disk Quota).
  - RAM allocation.

---

### 📋 Log Viewer & Notifications

- **Smart Log Viewer**: Bộ lọc log thông minh.
  - Hiển thị Error/Access logs theo thời gian thực.
  - Tìm kiếm và filter theo pattern.
- **Notification System**: Thông báo sự cố tự động.
  - Web Push notifications.
  - Chuông thông báo trên giao diện.
  - Tích hợp Telegram alerts.

---

### 🎨 UI/UX Enhancements

- **Dark/Light Mode**: Giao diện tùy chỉnh theo sở thích.
- **Global Search (Ctrl + K)**: Tìm kiếm nhanh website, database hoặc tính năng bất kỳ trong Panel.
- **System Health Badge**: Icon trạng thái (Xanh/Vàng/Đỏ) luôn hiển thị ở góc màn hình để theo dõi sức khỏe server.
- **Minimalism Design**: Phong cách thiết kế tối giản, sạch sẽ lấy cảm hứng từ Vercel và DigitalOcean.
- **Tailwind CSS Powered**: UI hiện đại, mượt mà và cực kỳ chuyên nghiệp.
- **Responsive Design**: Tương thích hoàn hảo trên mobile và tablet.
- **Modern UI**: Giao diện Single Page App (SPA) cực nhanh với Vue.js/Svelte.

---

### ⚡ Deployment Architecture

```
┌──────────────────────────────────────────────────────┐
│                   Panda v3.0                         │
├──────────────────────────────────────────────────────┤
│  Backend: Golang (Gin/Echo)                          │
│  Frontend: Vue.js/Svelte (embedded trong binary)     │
│  Database: Internal SQLite (Config)                  │
│  Result: 1 file binary duy nhất (~20-30MB)           │
└──────────────────────────────────────────────────────┘
```

- **Persistence**: Sử dụng SQLite để lưu cấu hình, không cần database server riêng cho Panel.
- **Auto-Backup DB**: Tự động backup file `panda.db` định kỳ lên Cloud. Khôi phục toàn bộ Panel chỉ với file binary + file .db.
- **Embedded Assets**: Toàn bộ Frontend được đóng gói vào trong binary (sử dụng `go:embed`), không lộ thư mục mã nguồn web.

**Triển khai đơn giản:**
1. Copy file `panda` lên server.
2. Chạy `./panda`.
3. Truy cập `http://your-ip/panda`.

---

### 📊 So sánh với các Control Panel khác

| Tiêu chí | Panda v3 | CyberPanel | aaPanel |
|----------|----------|------------|---------|
| **Thời gian cài đặt** | **< 10 giây** | 5 - 15 phút | 5 - 15 phút |
| Dung lượng cài đặt | ~20MB | >500MB | >300MB |
| Tiêu thụ RAM | ~20MB | >200MB | >150MB |
| Dependencies | 0 | Nhiều | Nhiều |
| Deployment | 1 file | Complex | Complex |

---

### � Migration từ v2.x

- **Backward Compatibility**: CLI commands vẫn hoạt động.
- **Config Migration**: Tự động import cấu hình từ v2.x.
- **Data Preservation**: Giữ nguyên toàn bộ websites và databases đã tạo.

---

> 💡 **Lưu ý**: Phiên bản 3.0 đang trong giai đoạn phát triển. Các tính năng có thể thay đổi trước bản release chính thức.